#include <bits/stdc++.h>
 
 using namespace std; 
 int main() { 
 ifstream fin ("Temp_Folder/new_file.cpp");
 string str;
 getline(fin, str);
 cout << str;
 getline(fin, str);
 cout << str;
 getline(fin, str);
 cout << str;
 getline(fin, str);
 cout << str;
 getline(fin, str);
 cout << str;
 getline(fin, str);
 cout << str;
 return 0; 
}